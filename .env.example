# JWT 签名密钥（生产环境必填，生成方式：openssl rand -hex 64）
JWT_SECRET=change-me-to-a-random-string

# 服务器端口
PORT=3000

# 运行环境
NODE_ENV=production

# TLS 证书校验：0 = 关闭（兼容 hosts 指向 CDN IP 的场景），1 = 开启严格校验
NODE_TLS_REJECT_UNAUTHORIZED=0
